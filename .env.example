# Copy to .env (gitignored) and fill in your own values.
# Get an API key at https://platform.openai.com/settings/organization/api-keys
# Note: a ChatGPT Plus/Pro subscription does NOT include API access -- API usage
# is billed separately on platform.openai.com.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.5
