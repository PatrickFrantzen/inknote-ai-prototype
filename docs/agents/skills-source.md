# Vendored skills source

The skills in `.claude/skills/` were copied from https://github.com/mattpocock/skills
at commit `3cca18b368ae95cdbdebbff572ccafa662551015` (imported 2026-09-08).

They are plain files owned by this repo, not a Claude Code plugin install, so
they can be edited freely. To pull updates later, re-copy the relevant skill
folders from that repo and diff by hand.
